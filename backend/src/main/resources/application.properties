server.port=${PORT:8080}

# --- Supabase Database Configuration ---
# Provide your Supabase Postgres connection string. Notice the '?sslmode=require' at the end.
spring.datasource.url=jdbc:postgresql://${SUPABASE_DB_HOST:db.oslhmctcqgszovthxjwx.supabase.co}:5432/postgres?sslmode=require
spring.datasource.username=${SUPABASE_DB_USER:postgres}
spring.datasource.password=${SUPABASE_DB_PASSWORD:@Nampa11y77}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.default-property-inclusion=non_null

logging.level.com.kaveri=INFO
